name: Manual Blog Draft Generation

on:
  workflow_dispatch:
    inputs:
      topic:
        description: 'Article archetype (random rotates, skipping recently covered angles)'
        required: false
        type: choice
        options:
          - random
          - performance
          - camera
          - battery
          - value
          - budget
          - flagship
          - gaming
          - midrange
          - brand_showdown
          - versus
        default: 'random'

permissions:
  contents: read

jobs:
  generate-blog:
    name: Generate E-E-A-T Blog Post
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-blog-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-blog-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate Draft For Editorial Review
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_PORT: ${{ secrets.DB_PORT }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          BLOG_TOPIC: ${{ github.event.inputs.topic || 'random' }}
          BLOG_STATUS: draft
        run: |
          python -u blog_manager.py --topic "$BLOG_TOPIC" --status "$BLOG_STATUS" --save
